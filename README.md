This is the template repository to generate RepoSense dashboard under `portfolio` mode.

Users can fork the repository, update the configuration and deploy the code portfolio website to Github Action. 
